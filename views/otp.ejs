<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verify</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');

        body {
            font-family: 'Inter', sans-serif;
        }

        .otp {
            text-align: center;
            font-size: 24px;
            width: 50px;
            height: 50px;
            margin-right: 10px;
            display: inline-block;
            border: 2px solid #e5e7eb;
            border-radius: 0.375rem;
        }
    </style>
</head>

<body class="bg-gradient-to-r from-green-400 via-blue-500 to-purple-600 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-md bg-white p-8 rounded-lg shadow-lg">
        <h2 class="text-2xl font-semibold text-center text-gray-800 mb-4">OTP Verify</h2>
        <form action="/otpVerify" method="POST">
            <div class="form-group text-center mb-4">
                <label for="otp" class="block text-sm font-medium text-gray-700 mb-2">OTP</label>
                <div class="flex justify-center space-x-2">
                    <input class="otp" type="text" name="otpB1" id="otp" required maxlength="1" />
                    <input class="otp" type="text" name="otpB2" id="otp" required maxlength="1" />
                    <input class="otp" type="text" name="otpB3" id="otp" required maxlength="1" />
                    <input class="otp" type="text" name="otpB4" id="otp" required maxlength="1" />
                    <input class="otp" type="text" name="otpB5" id="otp" required maxlength="1" />
                    <input class="otp" type="text" name="otpB6" id="otp" required maxlength="1" />
                </div>
            </div>
            <button type="submit"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">Verify</button>
        </form>
    </div>
</body>

</html>